Functions

#with return a arguement#
import random
def get_answer(answer_num):
    if answer_num == 1:
        return 'ydfes'
    elif answer_num == 2:
        return 'by'
    elif answer_num == 3:
        return 'great'
    elif answer_num == 4:
        return 'close'
    else:
        return 'non of'

print(get_answer(random.randint(1,6)))
  #get_answer = calls the function
  #(random.randit(1,6)) = passing the arguement to funciton between 1,6
  #print = print the return the value

None value
----------
  None == Null
  there is no Null now in python. now uses None insted of Null.
  print(type(None)) => will print "<class 'NoneType'>"
  print(type(Null)) => will throw the "NameError: name 'Null' is not defined" error

#example#
var = None 
if var is None:
    print('no value') => will print
elif var is not None:
    print('print var value')

#Named Parameters#
-------------------
/e.g in print
avoid sperating from from keywords like in line jump
print('Hello')
print('World')
#the output would look like this:#
Hello
World

use of Named parameters;
print('Hello', end='')
print('World')
#the output would look like this:#
HelloWorld

import random
for i in range (1,100):
    if  random.randint (0,1) == 0:
        print('T ', end='')
    else:
        print('S ', end= '')
T S T T S S T T S T T T T T T S S S S S T S T T T T S S T S T S S T T S T T S T T T T T S T S S T T S S T T S T S S S S S S S T S T T T S T T T S S T S T T S S S T T S S T T T T S T S S S S S T S T >>> 
there is no line jump between if and else printers

#Local and Global scope#
------------------------
def spam():
    global eggs
    print (eggs)  # Prints the global eggs 'Global'
    eggs = 'spam'  # Changing the Global variable 'Global' to 'spam'
    
def bacon():
    eggs = 'bacon'  # This is a local variable.
    print(eggs) #will  print 'bacon'. no global variable assign

def ham():
    global eggs
    eggs = 'ham changed global'
    print(eggs)  # This is the global variable. Because the ham hasn't assign any value as eggs its local yet.

eggs = 'global'  # This is the global variable.
spam()
print(eggs) # Prints the global value #result will be 'spam'
ham()
print(eggs)

#final takeaway, when a global variable changed from local, the global variable value will change forever.#


#TryCatch Exception handling#
=============================
def spam(divide_by):
    return 42 / divide_by

print(spam(2))
print(spam(12))
print(spam(0))
print(spam(1))

- result -
21.0
3.5
print(spam(0))
ZeroDivisionError: division by zero

#exception handling#
---------------------
def deviation(value):
    try:
        return 46/value
    except ZeroDivisionError:
        return print('Error: Invalid argument.')
print(deviation(32))    # 1.4375
print(deviation(43))    # 1.069767441860465
print(deviation(0))     # result will be like Error: Invalid argument.
print(deviation(15))    # 3.066666666666667

//if the try went to except because of an error it won't go back to the try clause again. it will continue move.//
e.g.
----
def spam(divide_by):
    return 42 / divide_by

try:
    print(spam(2))
    print(spam(12))
    print(spam(0))
    print(spam(1))
except ZeroDivisionError:
    print('Error: Invalid argument.')
print('no more tries. its an end')

#result will be like:#
21.0
3.5
Error: Invalid argument.
no more tries. its an end
>>> 
